@model MessageViewModel;
@{
    ViewData["Title"] = "Успех";
}
    <div class="info info-@Model.Type">
        <h2 class="info-tittle">@Model.Tittle</h2>
        <div class="info-body">
            <h3>@Model.Subtittle</h3>
            <p>@Model.Message</p>
            <img src="~/img/@(Model.Type).svg" alt="Alternate Text"/>
        </div>
    </div>
